# Line Shifter

Install the plugin with lazy package manager:
```lua
return {
  "A2Kayise/line-shifter.nvim",
  name = "line-shifter",
  config = function()
    require("line-shifter").setup()
  end
}
