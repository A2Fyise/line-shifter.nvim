# Line Shifter

Install the plugin with lazy package manager:
```lua
return {
  "A2Fyise/line-shifter.nvim",
  name = "line-shifter",
  config = function()
    require("line-shifter").setup()
  end
}
